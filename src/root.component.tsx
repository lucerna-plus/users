export default function Root(props) {
  //console.log(props);
  // return <section>{props.name} {JSON.stringify(props)} is mounted! USERS !!!</section>;
  return <section>{props.name} is mounted! <b>USER !!!</b></section>;
}
